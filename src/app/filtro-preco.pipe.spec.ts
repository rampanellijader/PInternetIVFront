import { FiltroPrecoPipe } from './filtro-preco.pipe';

describe('FiltroPrecoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPrecoPipe();
    expect(pipe).toBeTruthy();
  });
});
