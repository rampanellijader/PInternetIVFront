import { FiltroProdutoPipe } from './filtro-produto.pipe';

describe('FiltroProdutoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroProdutoPipe();
    expect(pipe).toBeTruthy();
  });
});
